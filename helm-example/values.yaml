# helm-example/values.yaml - Sample values for a demo application

replicaCount: 2

image:
  repository: nginx
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

persistence:
  enabled: true
  storageClass: "longhorn"
  size: 5Gi
